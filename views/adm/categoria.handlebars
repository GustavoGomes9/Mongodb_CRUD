{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
<div class="card">
    <div class="card-body">
        <form action="./categoria/nova" method="POST">
            <label>Nome:</label>
             <input type="text" class="form-control" placeholder="Nome" maxlength="100" name="nome">
            <label>Slug:</label>
            <input type="text" class="form-control" placeholder="Nome" maxlength="100" name="slug">
            <br/>
            <button type="submit" class="btn btn-outline-primary">Cadastrar</button>
        </form>
    </div>
</div>